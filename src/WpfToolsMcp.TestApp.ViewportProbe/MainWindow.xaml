<Window x:Class="WpfToolsMcp.TestApp.ViewportProbe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        AutomationProperties.AutomationId="ViewportProbe_Window"
        Title="WPF Tools MCP ViewportProbe TestApp"
        Width="720"
        Height="520"
        MinWidth="360"
        MinHeight="280"
        ResizeMode="CanResize"
        WindowStartupLocation="Manual"
        Left="160"
        Top="140"
        Background="White"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Grid x:Name="ViewportRoot"
          AutomationProperties.AutomationId="ViewportProbe_Root"
          Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="ViewportStatus"
                   Grid.Row="0"
                   AutomationProperties.AutomationId="ViewportProbe_Status"
                   Margin="12"
                   Text="Viewport pending"
                   TextWrapping="Wrap" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Margin="12,0,6,12"
                    Background="#FFE8EDF2"
                    BorderBrush="#FF65717C"
                    BorderThickness="1" />
            <Border Grid.Column="1"
                    Margin="6,0,12,12"
                    Background="#FFF3E8D5"
                    BorderBrush="#FF8A6D3B"
                    BorderThickness="1" />
        </Grid>
    </Grid>
</Window>
